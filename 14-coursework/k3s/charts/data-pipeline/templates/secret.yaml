# MinIO credentials secret — створюється вручну або через Sealed Secrets
# kubectl create secret generic minio-credentials \
#   --from-literal=rootUser=minioadmin \
#   --from-literal=rootPassword=minioadmin123 \
#   -n staging
